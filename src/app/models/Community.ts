import { User } from './User';

export interface Community {
  id: number;
  name: string;
  user: User;
  // posts: number;
  // isMember: boolean;
  // dateCreated: Date;
  // description: string;
  // image: string;
}
