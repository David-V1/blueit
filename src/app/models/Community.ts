import { User } from './User';

export interface Community {
  id: number | null;
  name: string;
  // posts: number;
  // isMember: boolean;
  // dateCreated: Date;
  // description: string;
  // image: string;
}
